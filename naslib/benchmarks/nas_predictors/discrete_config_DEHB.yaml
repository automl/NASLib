seed: 0
optimizer: bananas
search_space: nasbench201
dataset: cifar10
out_dir: run

search:
  seed: 0
  checkpoint_freq: 10 # 1000
  fidelity: 200
  budgets: 4000
  eta: 3
  max_budget: 512
  min_budget: 12
  epsilon: 1e-6
  predictor_type: var_sparse_gp
  top_n_percent: 0.3
  enc_dim: 6   #dimensions of the output fix.
  max_mutations: 1 #nummber of gens that mutate if it is mutating range(0 to enc_dim) but maybe to make it better (1 to 4)??
  crossover_prob: 0.5   #probabilty that a gen is choosen from mutate range(0.4 to 0.9)
  mutate_prob: 0.1 #probability of mutation  range(0.05 to 0.7)
  n_process: 1
